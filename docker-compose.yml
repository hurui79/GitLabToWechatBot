version: '3.8'

services:
  gitlab-wechat-bot:
    build: .
    container_name: gitlab-wechat-bot
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=5000
      - DB_PATH=/app/data/webhooks.db
      - LOG_RETAIN_DAYS=30
